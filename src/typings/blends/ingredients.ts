type SimpleIngredients = {
  collection: string;
  schema: string;
  template: string;
  image: string;
};

type SimpleAssetIngredient = {
  assetid: number;
  template: number;
};

export type { SimpleIngredients, SimpleAssetIngredient };
